# GitHub root site 404 (expected)

`https://zax-million.github.io/` returns 404 because this project is a **project site**:

`https://zax-million.github.io/zax-kodelex/`

That is expected. Do not “fix” the account root unless you intentionally publish a user/organization site from a `ZAX-MILLION.github.io` repository.
